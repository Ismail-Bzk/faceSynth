# Configuration npm pour le projet FaceSynth

# Utiliser les versions exactes (recommandé pour production)
save-exact=true

# Timeout plus long pour les installations lentes
fetch-timeout=120000

# Legacy peer deps (si problèmes de dépendances)
legacy-peer-deps=true
